node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.vercel

# pnpm is the only supported package manager (see packageManager in
# package.json). Ignore competing lockfiles so a stray `npm install`
# can't re-commit one and make Vercel's package-manager detection
# ambiguous again.
package-lock.json
yarn.lock
