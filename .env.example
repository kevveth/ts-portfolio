# GitHub classic personal access token for the contributions graph.
# Create at https://github.com/settings/tokens with `read:user` scope only.
# Required at build time only — never shipped to the client bundle.
GITHUB_TOKEN=
